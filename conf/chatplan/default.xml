<?xml version="1.0" encoding="utf-8"?>
<include>
  <context name="default">

<!--
    <extension name="demo">
      <condition field="to" expression="^(.*)$">

	<action application="reply" data="Hello, you said: ${_body}"/>
      </condition>
    </extension> -->

    <extension name="demo">
      <condition field="to" expression="^(.*)$">
	<action application="python" data="OpenBTS_Send_SMS_Plain ${to}|${from}|${_body}"/>
      </condition>
    </extension>

  </context>
</include>
